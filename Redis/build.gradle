task execRedis {
  doLast {
    exec {
      commandLine 'cmd', '/c', 'redis-server.exe'
    }
  }
}

task stopRedis {
  doLast {
    exec {
      commandLine 'cmd', '/c', 'Taskkill /IM redis-server.exe /F'
    }
  }
}

